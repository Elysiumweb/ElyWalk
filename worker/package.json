{
  "name": "elywalk-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend de confiance ElyWalk (attestation Play Integrity, AdMob SSV, push FCM) sur Cloudflare Workers — gratuit, sans dépendance runtime.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
